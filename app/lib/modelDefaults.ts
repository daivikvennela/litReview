// Default model id used when no per-task / per-request model is selected.
// Keep in sync with frontend/src/lib/modelCatalog.ts (DEFAULT_MODEL_ID).
export const DEFAULT_MODEL_ID = "openrouter/free";
